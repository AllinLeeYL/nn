\documentclass[a4paper, 11pt]{article}

\usepackage[hidelinks]{hyperref} % 超链接
\usepackage[paper=a4paper, left=2cm, right=2cm, top=3cm, bottom=3cm]{geometry} % 页面排版
\usepackage[UTF8]{ctex} % 中文支持
\usepackage[divpsnames, svgnames, x11names]{xcolor} % 颜色
\usepackage{listings} % 代码
\usepackage{graphicx} % 插入图片
\usepackage{fancyhdr} % head and foot
\usepackage{enumitem} % 列表
\usepackage{amsmath} % 数学公式

\newcommand{\emphasize}[1]{\emph{#1}} % 强调
\newcommand{\keyword}[1]{\textbf{#1}} % 关键词
\newcommand{\refcode}[1]{Code \ref{#1}} % 引用代码
\renewcommand{\lstlistingname}{Code} % 代码label
\newcommand{\reffig}[1]{图\ref{#1}} % 引用图片
\newcommand{\reftab}[1]{表\ref{#1}} % 引用表格
\newcommand{\refeq}[1]{公式\ref{#1}} % 引用公式

% 列表样式
\setitemize{topsep=0em, itemindent=3em, itemsep=0em}
\setenumerate{topsep=0em, itemindent=3em, itemsep=0em}
% header and foot
\fancyhf{}
\fancyhead[L]{\leftmark}
\fancyfoot[R]{\thepage}
\pagestyle{fancy}
% 代码样式
\lstset{
	language=Python,
	otherkeywords={keyword1, keyword2},
	basicstyle=\small,
    keywordstyle=\color[RGB]{138, 41, 1}\bfseries,
    identifierstyle=,
    tabsize=4,
    commentstyle=\color{gray},
    backgroundcolor=\color[RGB]{247, 247, 247},
    stringstyle=\ttfamily,
    breaklines=true, % 代码自动换行，避免超出边框
    frame=trbl,
    numbers=left,
	numberstyle=\tiny\color{gray}
}